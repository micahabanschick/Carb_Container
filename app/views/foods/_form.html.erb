<%= form_for([@user, @food]) do |f| %>

  <% if @food.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@food.errors.count, "error") %> prohibited this food from being saved:
      </h2>

      <ul>
      <% @food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :name %><br>
    <%= f.text_field :name %></div>
  <br>

  <div>
    <%= f.label :brand, "Brand" %><br>
    <%= f.text_field :brand %></div>
  <br>

  <div>
    <%= f.label :carb_count, "Carbs (g)" %><br>
    <%= f.number_field :carb_count %></div>
  <br>  

  <div>
    <%= f.label :fiber_count, "Fiber (g)" %><br>
    <%= f.number_field :fiber_count %></div>
  <br>

  <div>
    <%= f.label :protein_count, "Protein (g)" %><br>
    <%= f.number_field :protein_count %></div>
  <br>

  <div>
    <%= f.label :fat_count, "Fat (g)" %><br>
    <%= f.number_field :fat_count %></div>
  <br>

  <div>
    <%= f.label :calorie_count, "Calories (kcal)" %><br>
    <%= f.number_field :calorie_count %></div>
  <br>

  <div>
    <%= f.submit %>
  </div>
<% end %> 
