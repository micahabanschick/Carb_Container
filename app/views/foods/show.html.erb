<p id="notice"><%= notice %></p>


<div class="center">
  <div class="info">
    <% render 'food' %>
    <%= form_for @user_food, :url => add_food_path do |f| %>
        <%= f.hidden_field :user_id %><br />
        <%= f.hidden_field :food_id %><br />
        <%= f.submit "Add", class:"btn btn-light" %>
    <% end %>
    <p><%= link_to 'Back', foods_path, class: "btn btn-primary btn-light" %></p>
    <p><%= link_to 'Edit', edit_food_path(@food) %></p>
  </div>
</div>
